# Azure AI Foundry Project Configuration
# Copy this file to .env and fill in your values

# Required: Your Azure AI Foundry project endpoint
# Format: https://<project-name>.<region>.api.azureml.ms
# Find this in your Foundry project overview page
PROJECT_ENDPOINT=https://your-project-name.eastus.api.azureml.ms

# Optional: Model deployment name in your Azure OpenAI resource
# Common values: gpt-4o-mini, gpt-4o, gpt-4-turbo
# Default: gpt-4o-mini if not specified
MODEL_DEPLOYMENT_NAME=gpt-4o-mini

# Optional: Bing Custom Search connection ID
# If not provided, the script will auto-discover it from your project
# Format: /subscriptions/<sub-id>/resourceGroups/<rg>/providers/.../connections/<name>
# BING_CONNECTION_ID=/subscriptions/your-subscription-id/resourceGroups/your-rg/providers/Microsoft.CognitiveServices/accounts/your-ai-service/projects/your-project/connections/bing-custom-search

# Authentication: Uses DefaultAzureCredential
# Make sure you're logged in: az login
# Or use managed identity in production

# Optional: MCP Server configuration (for address change tool)
# Uncomment and fill in these values if you want to add MCP tools to the agent
# MCP_SERVER_URL=https://func-api-lwtfpn4ptwceg.azurewebsites.net/runtime/webhooks/mcp/sse
# MCP_EXTENSION_KEY=your-mcp-extension-system-key
